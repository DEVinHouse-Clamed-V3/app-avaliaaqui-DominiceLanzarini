import { StyleSheet } from "react-native";

export const globalStyles = StyleSheet.create({
    input: {
        borderWidth: 1,
        borderColor: '#0a4e8e', // Borda azul escuro
        padding: 15,
        borderRadius: 10,
        marginBottom: 20,
        backgroundColor: '#fff', // Fundo branco nos inputs
        color: '#1074d2', // Texto azul
      }
})